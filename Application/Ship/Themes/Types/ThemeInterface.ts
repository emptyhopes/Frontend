interface ThemeInterface {
  header: {
    background: {
      color: string;
    };
  };

  wrapper: {
    background: {
      color: string;
    };
  };

  footer: {
    background: {
      color: string;
    };
  };

  button: {
    background: {
      color: string;
    };
  };
}

export { ThemeInterface };
